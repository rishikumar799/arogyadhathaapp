import React from 'react';
import Icon from './Icon';

export default function GovIdIcon({ size = 20, color }: { size?: number; color?: string }) {
  // Placeholder using icon symbol - replace with custom SVG if needed
  return <Icon name="id-card" size={size} color={color} />;
}
