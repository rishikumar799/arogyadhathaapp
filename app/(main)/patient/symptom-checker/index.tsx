import React from 'react';
import { ThemedView } from '@/components/themed-view';
import { ThemedText } from '@/components/themed-text';

export default function SymptomChecker() {
  return (
    <ThemedView>
      <ThemedText>Symptom checker</ThemedText>
    </ThemedView>
  );
}
