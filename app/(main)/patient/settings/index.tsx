import React from 'react';
import { ThemedView } from '@/components/themed-view';
import { ThemedText } from '@/components/themed-text';

export default function PatientSettings() {
  return (
    <ThemedView>
      <ThemedText>Patient settings</ThemedText>
    </ThemedView>
  );
}
